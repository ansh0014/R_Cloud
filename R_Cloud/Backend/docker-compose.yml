version: '3.8'

services:
  ai-validation-agent:
    build:
      context: ./ai-validation-agent
    ports:
      - "8000:8000"
    environment:
      - LLM_API_KEY=${LLM_API_KEY:-}
      - LLM_MODEL=${LLM_MODEL:-gpt-4-turbo-preview}
